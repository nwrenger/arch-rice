#!/usr/bin/env fish
source ~/.local/bin/menu-lib

set options "󰒃 Toggle" " Set location"
set choice (printf "%s\n" $options | walker --dmenu --nosearch --minwidth 250 --minheight 100)

switch $choice
    case "󰒃 Toggle"
        toggle-vpn
    case " Set location"
        alacritty --title "walker menu" -e fish -c "
            source ~/.local/bin/menu-lib
            menu-vpn-location
            show_done"
end
