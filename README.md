# arch-rice

Arch Linux + Hyprland setup. Catppuccin Mocha / Mauve.

## Fresh install

Install arch via `archinstall`. It is important that you select these options: `btrfs`, `hyprland`, `limine` and `sddm`. Also you need to install fish before running the install script via: `sudo pacman -Syu fish`.

```bash
curl -fsSL https://raw.githubusercontent.com/nwrenger/arch-rice/main/install.fish | fish
```

The installer will:

- Detect GPU/CPU and skip irrelevant packages
- Install paru
- Install all packages from `packages-aur.txt` + `packages-pacman.txt`
- Stow all home dotfiles
- Copy system configs
- Enable system + user services
- Set Fish as default shell

## Manual post-install steps

1. **Limine**: Set your disk's `PARTUUID` in `/boot/limine.conf`
   Find it with: `blkid -s PARTUUID -o value /dev/sdXY`
   Then: `sudo limine-enroll-config`

2. **Snapper**: If subvolume layout differs, run `sudo snapper -c root create-config /`

## Repo layout

```
arch-rice/
├── install.fish          ← run this on a fresh Arch install
├── export.fish           ← for me to export live system
├── packages.txt          ← all packages, tagged by hardware
│
├── home/                 ← GNU Stow packages (target: $HOME)
│   ├── hypr/             → .config/hypr/
│   ├── waybar/           → .config/waybar/
│   ├── alacritty/        → .config/alacritty/
│   ├── elephant/         → .config/elephant/
│   ├── fish/             → .config/fish/
│   ├── starship/         → .config/starship.toml
│   ├── walker/           → .config/walker/
│   ├── kvantum/          → .config/Kvantum/
│   ├── qt/               → .config/qt5ct/ + .config/qt6ct/
│   ├── gtk/              → .config/gtk-3.0/ + .config/gtk-4.0/
│   ├── fontconfig/       → .config/fontconfig/
│   ├── mako/             → .config/mako/
│   ├── swayosd/          → .config/swayosd/
│   ├── fastfetch/        → .config/fastfetch/
│   ├── btop/             → .config/btop/
│   ├── zed/              → .config/zed/
│   ├── nwg-look/         → .config/nwg-look/
│   ├── uwsm/             → .config/uwsm/
│   ├── systemd/          → .config/systemd/user/ (user services)
│   └── local-bin/        → .local/bin/ (custom scripts)
│
└── system/               ← copied to / with sudo (not stowed)
    ├── etc/
    │   ├── locale.conf
    │   ├── pacman.conf
    │   ├── snapper/configs/root
    │   ├── limine-snapper-sync.conf
    │   ├── sddm.conf.d/theme.conf
    │   ├── systemd/zram-generator.conf
    │   └── udev/rules.d/99-ddcci.rules
    ├── boot/
    │   └── limine.conf.template  ← PARTUUID placeholder, set manually
    └── sddm-theme/
        └── where_is_my_sddm_theme/
```

## Hardware tags in packages.txt

| Tag         | Skipped when                |
| ----------- | --------------------------- |
| `nvidia`    | No NVIDIA GPU detected      |
| `intel-cpu` | Not Intel CPU               |
| `amd-cpu`   | Not AMD CPU                 |
| `ddcci`     | User declines DDC/CI prompt |

## What's intentionally NOT exported

- `~/.local/share/keyrings/` — GNOME keyring secrets
- `~/.ssh/` — SSH keys
- `/etc/mullvad-vpn/` — VPN account/device credentials
- Snapper snapshot data in `/boot/` — auto-regenerated by limine-snapper-sync
